

## Clarify weekly progress label

Use the user's preferred phrasing.

### Change

Single string in `src/routes/_authed/results.tsx`, the section eyebrow above the Progress bar:

| Current | Proposed |
|---|---|
| `This week's draw` | `Tickets earned this week` |

No layout, styling, casing, data, or component changes. The existing `uppercase` class will render it as `TICKETS EARNED THIS WEEK`.

### Out of scope

Everything else on the page.

