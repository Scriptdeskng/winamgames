Plan to implement the requested light/dark mode theming:

1. Update global theme tokens in `src/styles.css`
   - Keep the existing dark token set as the default `:root` values.
   - Add an `html.light` override block that redefines all relevant color tokens for light mode: background, foreground, surfaces, cards, popovers, primary foreground, secondary, muted, accent, destructive foreground, border, input, sidebar, and shadows.
   - Add `color-scheme: light` for `html.light` so browser form controls match light mode.
   - Preserve emerald primary and existing semantic colors unless a light-mode contrast adjustment is needed.

2. Add the no-flash startup theme script in `src/routes/__root.tsx`
   - Insert the inline script inside `<head>` before `<HeadContent />`, so it runs before the stylesheet link generated by the route head.
   - Use `localStorage.getItem('winam-theme')` and `prefers-color-scheme` exactly as requested.
   - Keep the existing `<html className="dark">` default so admin and first paint remain dark unless the script applies `light`.

3. Create `src/hooks/useTheme.ts`
   - Add the requested `useTheme` hook with SSR-safe initialization.
   - Persist the selected theme to `localStorage`.
   - Toggle only the `light` class on `document.documentElement`; dark remains the default/no-light state.

4. Add the profile-only Appearance toggle in `src/routes/_authed/profile.tsx`
   - Import `Sun`, `Moon`, the existing `Switch` component, and the new `useTheme` hook.
   - Add an `AppearanceSection` component.
   - Place it near the bottom of the profile page, after the activity/how-to sections and directly above the logout button.
   - Render a row labeled `Dark mode`, with `Sun` when dark mode is active and `Moon` when light mode is active.
   - Bind the switch checked state to `theme === 'dark'` and call `toggle` on change.

5. Scope and verification
   - No TopBar changes.
   - No admin panel-specific UI changes.
   - No game logic, server functions, or database changes.
   - Run a build/typecheck after implementation to catch import or JSX issues.